#' Load a brain signature
#'
#' This function loads the approproate brain signature. It takes two arugments,
#' first is the name of the brain signatue; the second is the type of data.

#' @export

## What to do on how to code it

#### steps required to load a specific signature


loadBrainSig <- function(signarture){
  ct <- ifelse(signature == "F5", print(sigsBrain[1]), print("error"))
  }

sigsBrain[1]
f5 <- loadBrainSig("F5")



load

### index in the list
## grep the list
